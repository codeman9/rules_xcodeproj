#!/bin/bash

# libtool wrapper for rules_xcodeproj Build with Bazel mode.
# Passes through to real libtool so that static library archives (.a files)
# are created from swiftc_stub's .o files. The Xcode Preview system needs
# these archives to analyze static library targets for preview thunk linking.

set -euo pipefail

exec libtool "$@"
